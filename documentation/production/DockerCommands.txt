# build image in the Directory Bachelorarbeit/Code
Bachelorarbeit/Code$ docker build -t wsfnetwork:1.3 .

# run a container
docker run -d --name=WSFNetworkContainer wsfnetwork:1.3

# create volume
docker volume create wsfdata

# execute command in container
docker exec -it <container_id> /bin/bash

# copy files
#docker cp <SRC_HOST> <container_id>:<DEST_Container>

